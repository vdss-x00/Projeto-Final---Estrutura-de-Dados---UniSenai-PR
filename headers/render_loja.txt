//arquivo de referência para quando for inicializar a sistemaLoja();



#include <iostream>
#include <vector>
#include <string>

class Lampada{
    private:
        std::string nome;
        int lumens;
        int potencia;
        float preco;
    public:
        Lampada(std::string n, int l, int po, float pr) : 
            nome(n), lumens(l), potencia(po), preco(pr){}
            
        void renderizar const(){
            //lógica adicional aqui
        }
}


//dentro do .cpp desse código
std::vector<Lampada> listaDeProdutos;

    
    for (const auto& produto : listaDeProdutos) {
        produto.render(); // Chama a exibição de cada "componente"
    }